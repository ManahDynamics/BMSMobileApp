import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand
  static const Color primaryGreen = Color(0xFF1B6B3A);
  static const Color primaryBlue = Color(0xFF3A6EAC);

  // Neutral
  static const Color background = Color(0xFF1B6B3A);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Colors.white;
  static const Color textLightSecondary = Color(0xFFB3B3B3);

  // Semantic
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);

  // Input fields
  static const Color inputBackground = Color(0xFFF0F0F0);
  static const Color inputHint = Color(0xFF9CA3AF);

  // Overlay / Dropdown
  static const Color overlayBackground = Colors.white;
}